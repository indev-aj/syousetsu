import 'package:flutter/material.dart';

class LatestUpdateScreen extends StatelessWidget {
  const LatestUpdateScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}